// El operador ternario en JS es una forma corta y elegente de una estructura if...else

// Se llama ternario porque tiene tres opetadores, observamos la sintaxis
// condición ? valorSiVerdadero : valorFalso

// Ejemplo 1 

let age = 18;
let message = age >=18? "Eres mayor de edad" : "Eres menor de edad";
console.log(message);

